<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <corners android:radius="0dp" />
    <stroke
        android:width="1dp"
        android:color="@color/whiteCol" />

    <size
        android:width="250dp"
        android:height="30dp" />
</shape>